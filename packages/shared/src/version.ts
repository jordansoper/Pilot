/**
 * Re-exported so the CLI can echo `"version"` for `/api/health` and the app
 * can display it without a second import path. Bumped manually in Phase 1
 * via a release script (placeholder).
 */
export const SHARED_PACKAGE_VERSION = '0.0.0';
