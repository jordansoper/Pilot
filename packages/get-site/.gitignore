# Wrangler local state & logs
.wrangler/
.dev.vars

# Generated by `pnpm bundle` (postinstall hook) — install.sh is read from
# the repo root and embedded into the deployed worker for low-latency serving.
src/install-bundled.ts

# Build output (wrangler --outdir=dist)
dist/

# Dependency tree
node_modules/

# Coverage
coverage/

# OS noise
.DS_Store
Thumbs.db
