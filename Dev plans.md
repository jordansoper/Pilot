Review documents in app build plans

Upload to github, with download-able installers for android  windows, tacos

Able to install on windows, mac os, Linux with a terminal command (linux install cantaining all dependencies needed)

Pilot to be its own terminal app (similar to how warp works)

Snapshots active sessions when desktop client is closed, so they can open up again when restarted.

Remove requirement for tailscale, as there is a local connection included already